import { GameExperience } from "../components/game/GameExperience";

// Visual contract: retro pixel ocean, centered title/boat/waterline, submerged fixed CTA, HUD/game loop, and original OMNIQUIZ brand.
export default function Home() {
  return <GameExperience mode="daily" />;
}
